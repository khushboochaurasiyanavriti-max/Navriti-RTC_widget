# Widget Host Integration Sandbox

Integration sandbox for testing `@rtc-widget/react` component isolation with `platformId="platform-test2"`.

## 🚀 Running the App

```bash
cd widget-host-test
npm install
npm run dev
```

## ⚙️ Configuration

- **`platformId`**: `"platform-test2"` (Isolated platform scope).
- **`currentUser`**: `user-13` ("Khushboo13", role: `"admin"`).
- **`serverUrl`**: `http://localhost:5000`.
